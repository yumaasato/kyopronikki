#include <iostream>
#include <algorithm>
static const int N = 100;
using namespace std;

int main(void) {
  int n, a, sum, A[N+1];
  int p1, p2;
  int maxb = 0;

  cin >> n;

  for (int i = 0; i < n; i++) {
    cin >> A[i];
  }
  while (n) {
    
  }

  cout << maxb << endl;

  return 0;
}